---
title: Installation
description: How to install and configure ContextKit
---

## Install

```bash
npm install @runcontext/cli
```

That's it — one package, everything included. The `@runcontext/cli` package bundles the compiler, linter, tier engine, MCP server, site generator, introspector, and enricher.

## Global vs. local

```bash
# Global — use `context` command anywhere
npm install -g @runcontext/cli

# Local — use via npx or npm scripts
npm install -D @runcontext/cli
npx context --help
```

## Scaffold a new project

```bash
npx create-contextkit my-project
cd my-project
```

This creates a project with example metadata files and a `contextkit.config.yaml`.

## Verify installation

```bash
context --version
# 0.3.5

context --help
# Shows all 15 commands
```

## Database drivers

ContextKit supports DuckDB and PostgreSQL. For DuckDB introspection, you'll also need:

```bash
npm install duckdb
```

PostgreSQL support works via connection strings — no extra driver needed if you have `pg` available.

## Next steps

- [Quick Start](/getting-started/quick-start) — set up your first project
- [Tier System](/concepts/tiers) — understand Bronze, Silver, and Gold
- [CLI Overview](/cli/overview) — see all available commands
