---
title: ContextKit
description: "Tell your AI agent to build your semantic layer. ContextKit — the open-source CLI for introspecting databases, curating metadata, and serving AI-ready context via MCP."
template: splash
hero:
  tagline: Tell your AI agent to build your semantic layer. It uses ContextKit to introspect your database, scaffold metadata, and serve curated context to every AI tool in your stack via MCP.
  image:
    dark: ../../assets/logo-dark.svg
    light: ../../assets/logo-light.svg
  actions:
    - text: Get Started
      link: /getting-started/quick-start/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/erickittelson/ContextKit
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';

<div style="text-align:center;margin:1rem 0 2.5rem;">
  <div class="hero-install" onclick="navigator.clipboard.writeText('npm install -g @runcontext/cli').then(()=>{this.querySelector('.copy-badge').textContent='copied!'})">
    <span class="prompt">$ </span><span class="cmd">npm install -g @runcontext/cli</span>
    <span class="copy-badge">copy</span>
  </div>
</div>

## How It Works

Your agent runs ContextKit commands to build the semantic layer with you. You provide the business knowledge. The agent does the rest.

<div class="comparison">
  <div class="without">
    <h4>Without ContextKit</h4>
    <pre><code>-- "What's total revenue?"
SELECT AVG(revenue)     ← wrong aggregation
FROM transactions       ← no status filter
                        ← includes cancelled</code></pre>
  </div>
  <div class="with">
    <h4>With ContextKit</h4>
    <pre><code>-- "What's total revenue?"
SELECT SUM(revenue)     ← correct (semantic role)
FROM transactions
WHERE status='completed' ← required (guardrail)</code></pre>
  </div>
</div>

## Three Tiers to AI-Ready

<CardGrid>
  <Card title="Bronze — Discoverable" icon="document">
    Descriptions, ownership, security, grain. Achieved automatically via `context introspect`.
  </Card>
  <Card title="Silver — Trusted" icon="approve-check">
    Trust status, glossary, lineage, sample values. Achieved via `context enrich --target silver`.
  </Card>
  <Card title="Gold — AI-Ready" icon="star">
    Semantic roles, aggregation rules, guardrails, golden queries, business rules. Curated by you and your agent.
  </Card>
  <Card title="40 Lint Rules" icon="list-format">
    Schema validation, governance, security, data accuracy — all tier-aware with auto-fix.
  </Card>
</CardGrid>

## Everything in One Install

<CardGrid stagger>
  <Card title="15 CLI Commands" icon="setting">
    `setup`, `introspect`, `enrich`, `lint`, `fix`, `build`, `tier`, `explain`, `verify`, `serve`, `site`, `dev`, `init`, `rules`, `validate-osi`
  </Card>
  <Card title="MCP Server" icon="rocket">
    Serve your semantic layer to Claude Code, Cursor, Windsurf, and any MCP-compatible agent via stdio or HTTP.
  </Card>
  <Card title="Visual Studio Editor" icon="pen">
    `context dev --studio` — edit metadata in the browser, see tier updates live, save back to YAML.
  </Card>
  <Card title="9 Database Adapters" icon="laptop">
    PostgreSQL, DuckDB, MySQL, SQL Server, Snowflake, BigQuery, ClickHouse, Databricks, SQLite.
  </Card>
</CardGrid>

## Quick Start

```bash
npm install -g @runcontext/cli
context setup           # interactive wizard — database to metadata
context tier            # check your Bronze/Silver/Gold score
context serve           # expose to AI agents via MCP
```

<CardGrid>
  <LinkCard title="Introduction" description="How agents use ContextKit" href="/getting-started/introduction/" />
  <LinkCard title="Quick Start" description="Up and running in 5 minutes" href="/getting-started/quick-start/" />
  <LinkCard title="OSI Specification" description="The open standard behind it" href="/concepts/osi/" />
  <LinkCard title="MCP Server" description="Serve context to AI agents" href="/mcp/overview/" />
</CardGrid>
