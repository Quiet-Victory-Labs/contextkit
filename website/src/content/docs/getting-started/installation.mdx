---
title: Installation
description: "Install RunContext CLI for metadata governance and AI-ready semantic planes. Supports DuckDB, PostgreSQL, MySQL, Snowflake, BigQuery, and 4 more databases."
---

## Install

```bash
npm install @runcontext/cli
```

That's it — one package, everything included. The `@runcontext/cli` package bundles the compiler, linter, tier engine, MCP server, site generator, introspector, and enricher.

## Global vs. local

```bash
# Global — use `context` command anywhere
npm install -g @runcontext/cli

# Local — use via npx or npm scripts
npm install -D @runcontext/cli
npx context --help
```

## Scaffold a new project

```bash
npx create-runcontext my-project
cd my-project
```

This creates a project with example metadata files and a `runcontext.config.yaml`.

## Verify installation

```bash
context --version
# 0.3.5

context --help
# Shows all 16 commands
```

## Database drivers

RunContext supports 9 database engines. Each uses an optional peer dependency — install only the drivers you need:

| Database | Driver Package |
|---|---|
| DuckDB | `npm install duckdb` |
| PostgreSQL | `npm install pg` |
| MySQL | `npm install mysql2` |
| SQL Server | `npm install mssql` |
| SQLite | `npm install better-sqlite3` |
| Snowflake | `npm install snowflake-sdk` |
| BigQuery | `npm install @google-cloud/bigquery` |
| ClickHouse | `npm install @clickhouse/client` |
| Databricks | `npm install @databricks/sql` |

The setup wizard also auto-detects databases from your existing MCP server configs (Claude Code, Cursor, VS Code, Windsurf, Claude Desktop).

**Smart driver detection:** If you try to connect to a database without the driver installed, the `context setup` wizard will detect this and offer to install it automatically.

## Next steps

- [Quick Start](/getting-started/quick-start) — set up your first project
- [Tier System](/concepts/tiers) — understand Bronze, Silver, and Gold
- [CLI Overview](/cli/overview) — see all available commands
