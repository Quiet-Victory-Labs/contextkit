{
  "name": "runcontext",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest",
    "typecheck": "tsc -b && tsc --noEmit -p packages/cli && tsc --noEmit -p packages/site && tsc --noEmit -p packages/mcp && tsc --noEmit -p create-contextkit",
    "clean": "pnpm -r run clean",
    "release": "pnpm build && pnpm -r publish --no-git-checks"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
