{
  "name": "contextkit",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest",
    "typecheck": "tsc -b",
    "clean": "pnpm -r run clean",
    "release": "pnpm build && pnpm -r publish --no-git-checks"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
